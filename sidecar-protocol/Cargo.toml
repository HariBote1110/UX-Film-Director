[package]
name = "uxfd-sidecar-protocol"
version = "0.1.0"
edition = "2021"

[lib]
name = "uxfd_sidecar_protocol"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
