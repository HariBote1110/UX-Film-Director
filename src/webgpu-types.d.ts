// WebGPU の型定義（navigator.gpu / GPUDevice など）。
// 従来は pixi.js が推移的に @webgpu/types を提供していたが、PixiJS 排除計画
// Phase 5 で pixi.js を削除したため、devDependency の @webgpu/types を
// ここで明示的に参照する。
/// <reference types="@webgpu/types" />
