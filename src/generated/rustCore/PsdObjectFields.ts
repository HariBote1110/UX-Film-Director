// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { LipSyncSetting } from "./LipSyncSetting";
import type { PsdLayerNodeFields } from "./PsdLayerNodeFields";
import type { PsdLayerStruct } from "./PsdLayerStruct";
import type { PsdWorldPlacement } from "./PsdWorldPlacement";

/**
 * `psd` kind の編集モデル。JSON 保存対象の `src/types.ts` の `PsdObject`
 * と完全に shape 互換である。`file?: File` と各 `rootLayer` node の
 * `textureSource?: ImageBitmap` だけはブラウザ/GPU の非 JSON runtime 値で、
 * TS が保存前に除去するため本編集モデルには含めない。
 *
 * `activeLayerIds` はキー集合がレイヤーIDに依存し動的だが、決定的な JSON
 * キー順を保証するため `HashMap` ではなく `BTreeMap` を使う。保存済み
 * ファイルの既存キー順（挿入順）とはズレうるが、消費側
 * （`Object.entries`/`Record` として読む TS コード、および
 * `projectFile.test.ts` の `toEqual` によるラウンドトリップ検証）はすべて
 * キー順に依存しない構造比較のため、バイト互換ではなく shape 互換で足りる
 * （このバッチCで実測・確認済み）。
 */
export type PsdObjectFields = { src: string, filePath?: string | null, width: number, height: number, scale: number, rootLayer?: PsdLayerNodeFields | null, activeLayerIds?: { [key in string]: boolean } | null, lipSync?: LipSyncSetting | null, worldPlacement?: PsdWorldPlacement | null, layerTree?: Array<PsdLayerStruct> | null, };
